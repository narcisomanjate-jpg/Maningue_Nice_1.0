// Firebase sync has been removed for the offline-only distribution.
// This file remains as a no-op placeholder to avoid import errors in older branches.

export const useFirebase = () => {
  return {} as const;
};