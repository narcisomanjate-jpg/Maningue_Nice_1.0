// Arquivado: useFirebase.ts (removido da árvore principal - usado para sincronização com Firebase)
// Este arquivo foi arquivado pois a versão atual é 100% offline.

// Original (stub/no-op) preservado para referência:
export const useFirebase = () => {
  return {} as const;
};
